# .beads/issues.jsonl is a FULL-STATE export sorted by id, not append-only.
# Existing lines are modified in place. Do not set merge=union: union silently
# concatenates conflicting rewrites and has doubled whole Beads exports in real
# repos. Default git conflicts are loud and safer until Beads has a semantic
# merge driver. See cmdrvl-cli br-1zy4 / br-1rs1.
